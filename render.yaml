services:
  - type: web
    name: trivia-jam
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build -w packages/shared && npm run build -w packages/server
    startCommand: npm run start -w packages/server
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
